function MovieDesc({title, description}) {

    return (
        <div className="descript_container">
            <h5>{title}</h5>
            <p>{description}</p>
        </div>
    )
}

export default MovieDesc;